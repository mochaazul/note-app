import React from "react"
type Props = {
  dataSource: any
  counter: string
  isComplete?: boolean // nullable
}

const TestComponent: React.FC<Props> = ({dataSource, counter, isComplete}) => {

  return <>
  </>
}

export default TestComponent